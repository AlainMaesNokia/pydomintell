"""Setup file for domintell package."""
from setuptools import setup

setup(name='pydomintell',
      version='0.1',
      description='Python API for talking to a Domintell gateway',
      url='https://github.com/',
      author='Alain Maes',
      license='MIT',
      install_requires=[],
      packages=['domintell'],
      zip_safe=True)
